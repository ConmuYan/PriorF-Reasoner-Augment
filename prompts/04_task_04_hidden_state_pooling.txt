现在执行 Task 4。只做这一个任务，不要越界。

目标：
实现 llm/hidden_state_pooling.py，并为其写严格边界测试。

必须创建或修改的文件：
- llm/hidden_state_pooling.py
- tests/test_hidden_state_pooling.py

硬性要求：
1. 禁止使用 hidden_states[:, -1, :]
2. 必须根据 attention_mask 计算最后一个有效 token 的绝对位置
3. 必须兼容 left padding 和 right padding
4. 必须对 all-pad 行 fail closed
5. 必须写测试覆盖：
   - left padding
   - right padding
   - 单条序列
   - batch 序列
   - 全 padding 非法输入
6. 不要实现 cls_head
7. 不要实现 head scoring
8. 不要实现 trainer

极其重要的限制：
- 不允许使用 `sequence_lengths = attention_mask.sum(dim=1) - 1` 作为 left padding 的绝对索引
- 必须实现真正的 mask-aware absolute indexing

建议：
- 先构造 positions tensor
- 再通过 mask 求每行最后一个有效 token 的绝对位置
- 最后做 gather

交付时必须输出：
1. 修改文件列表
2. pooling 的核心索引逻辑
3. 为什么它对 left / right padding 都正确
4. fail-closed 的触发条件
5. 测试覆盖了哪些边界
6. 还没有做什么

完成后停下，等我审核，不要继续 Task 5。